---
- name: Copia instalador de Firefox nightly, para laboratorio de SSL
  hosts: fisicos
  tasks:
    - name: Copia instalador de Firefox nightly
      unarchive:
        src: http://{{ filesrv }}/vms/firefox-58.0a1.en-US.linux-x86_64.tar.bz2
        mode: 0775
        owner: "{{ ansible_user }}"
        dest: "{{ ansible_env.HOME }}/"
        remote_src: yes